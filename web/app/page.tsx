export default function Home() {
  return (
    <main className="shell">
      <section className="panel">
        <p className="eyebrow">Minecraft Seed Finder</p>
        <h1>Client-side seed search, powered by WASM.</h1>
        <p>
          Phase 0 establishes the monorepo, engine workspace, and Next.js app
          shell. The Cubiomes WASM worker pipeline will land in the next phase.
        </p>
      </section>
    </main>
  );
}
